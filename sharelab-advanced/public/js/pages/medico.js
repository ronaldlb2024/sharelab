/*
 * Lógica da página do médico.
 *
 * Esta página é um placeholder para a implementação futura de conexão
 * peer‑to‑peer (WebRTC) e exibição dos dados recebidos do paciente.
 * No momento, a função simplesmente avisa que a funcionalidade está
 * em construção. Para habilitar o P2P, será necessário incluir as
 * bibliotecas do Firebase para sinalização e implementar a troca de
 * offers/answers conforme o design do projeto.
 */

const connectBtn = document.getElementById('btnConnect');

if (connectBtn) {
  connectBtn.addEventListener('click', () => {
    alert('Funcionalidade de conexão ainda não implementada. Aguarde futuras versões.');
  });
}